services:
  redis:
    image: mirror.gcr.io/library/redis:7-alpine
    container_name: dsc-photobox-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

# -------------------------------------------------------------
# PostgreSQL Database (Lokal - Opsional)
# Jika ingin menggunakan database lokal, silakan uncomment service ini
# -------------------------------------------------------------
# db:
#   image: mirror.gcr.io/library/postgres:15-alpine
#   container_name: dsc-photobox-db
#   restart: unless-stopped
#   ports:
#     - "5432:5432"
#   environment:
#     POSTGRES_USER: postgres
#     POSTGRES_PASSWORD: HTpdrRgyZeXZN0lv
#     POSTGRES_DB: dsc_photobox
#   volumes:
#     - postgres_data:/var/lib/postgresql/data

volumes:
  redis_data:
  # postgres_data:
